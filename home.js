const MAX_TASKS=2;const demo=[{name:"Parent",hours:6,color:"#c4a35a",tasks:[{text:"Homework hour",done:false}]},{name:"Work",hours:10,color:"#7cb87a",tasks:[{text:"Ship the one thing",done:false}]},{name:"Self",hours:3,color:"#8aa7c4",tasks:[]}];
function renderDemo(){const root=document.getElementById("demo-app");root.innerHTML=demo.map((r,i)=>`<article class="box" style="border-top-color:${r.color}"><span class="hrs">${r.hours}h</span><h3>${r.name}</h3><label class="limit">Hours this week <input type="number" min="0" max="40" step="0.5" value="${r.hours}" data-h="${i}" /></label><ul class="tasks">${r.tasks.map((t,j)=>`<li><input type="checkbox" data-t="${i}:${j}" ${t.done?"checked":""} /><span style="${t.done?"text-decoration:line-through;color:#8a9a8c":""}">${esc(t.text)}</span></li>`).join("")}</ul>${r.tasks.length<MAX_TASKS?`<div class="add"><input data-n="${i}" placeholder="Add a task…" /><button data-a="${i}">Add</button></div><p class="limit">${r.tasks.length}/${MAX_TASKS} tasks in the demo</p>`:`<p class="limit">Demo cap: ${MAX_TASKS} tasks. Full app is unlimited.</p>`}</article>`).join("");root.querySelectorAll("[data-h]").forEach(inp=>{inp.onchange=()=>{demo[+inp.dataset.h].hours=Number(inp.value||0);renderDemo();};});root.querySelectorAll("[data-t]").forEach(cb=>{cb.onchange=()=>{const[i,j]=cb.dataset.t.split(":").map(Number);demo[i].tasks[j].done=cb.checked;renderDemo();};});root.querySelectorAll("[data-a]").forEach(btn=>{btn.onclick=()=>{const i=+btn.dataset.a;const inp=root.querySelector(`[data-n="${i}"]`);const text=(inp.value||"").trim();if(!text||demo[i].tasks.length>=MAX_TASKS)return;demo[i].tasks.push({text,done:false});renderDemo();};});root.querySelectorAll("[data-n]").forEach(inp=>{inp.onkeydown=e=>{if(e.key==="Enter"){const i=+inp.dataset.n;const text=inp.value.trim();if(!text||demo[i].tasks.length>=MAX_TASKS)return;demo[i].tasks.push({text,done:false});renderDemo();}};});}
function esc(s){return String(s).replace(/[&<>]/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;"}[c]));}
renderDemo();
